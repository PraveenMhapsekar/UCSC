int i;
int j=14;

main() {

	for(     ;
           j--;
           read('-' - '-' ,
									i+++"hello, world!\n",  
									'/' / '/')
	);

}

read (j,i,p)
{ 
  // ssize_t write(int fd, const void *buf, size_t count);
   write ( 1, i ,1);
}
